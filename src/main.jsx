import React from "react";
import ReactDOM from "react-dom/client";
import SheepySleep from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SheepySleep />
  </React.StrictMode>
);
